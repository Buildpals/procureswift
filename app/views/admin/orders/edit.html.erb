<div class="container">

  <div class="row mt-3">

    <div class="col-md-8 mx-auto">
      <div class="card mb-3">
        <div class="card-body">
          <h5 class="card-title">
            Editing Order
          </h5>

          <%= render 'form', order: @order %>
        </div>
        <div class="card-body">
          <%= link_to 'Show', admin_order_path(@order), class: 'card-link' %>
          <%= link_to 'Back', admin_orders_path, class: 'card-link'  %>
        </div>
      </div>
    </div>
  </div>

</div>
